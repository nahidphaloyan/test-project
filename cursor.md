# Cursor

This file is project context for Cursor.

## Project

- Name: test-project
- Purpose: (add a short description)
- Stack:
  - Runtime: Node.js
  - Language: TypeScript
  - Framework: Next.js (App Router)
  - Package manager: npm

## Conventions

- Use TypeScript throughout; avoid `any` unless there is a clear reason.
- Prefer the Next.js App Router (`app/`), Server Components by default, and Client Components only when interactivity or browser APIs are needed.
- Put API routes under `app/api/`.
- Keep UI, data fetching, and types in clear, small modules.
- Prefer small, focused changes.
- Match existing file structure and naming.
- Do not commit unless asked.

## Agent notes

- Explain what you changed in plain language.
- Ask before destructive git operations.
