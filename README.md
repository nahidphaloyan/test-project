# test-project

A Node.js test project. The intended stack is **TypeScript** and **Next.js** (App Router). Right now the repo is a basic npm package with `dotenv`.

## Stack

- Runtime: Node.js
- Language: TypeScript
- Framework: Next.js (App Router)
- Package manager: npm

## Setup

```bash
npm install
```

Copy environment variables into a `.env` file as needed. This project uses [`dotenv`](https://www.npmjs.com/package/dotenv).

## Scripts

| Script | Description |
| --- | --- |
| `npm test` | Placeholder (no tests yet) |

Next.js scripts such as `dev`, `build`, and `start` will be added when the app is scaffolded.

## License

ISC
